# Backend base URLs. For local dev the backend runs on :8000.
# WS base is derived from the API base if omitted (http->ws, https->wss).
VITE_API_BASE=http://localhost:8000
VITE_WS_BASE=ws://localhost:8000
